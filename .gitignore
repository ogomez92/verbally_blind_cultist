# Decompiled game code: copyrighted, reference only, never committed or shipped.
game/

# Third-party downloads and build outputs.
vendor/
dist/
bin/
obj/
*.user
.vs/

# release/ is the packaged payload and IS committed: the plugin references the game's
# own DLLs, so CI cannot build it. package.ps1 restages it, the release workflow zips it.
!release/
release/readme.html
